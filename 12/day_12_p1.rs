
use std::fs;
use std::vec::Vec;

fn main() {
    let file_path = "day12.input";
    let file = fs::read_to_string(file_path).unwrap();
    
    let mut score: u32 = 0;
    for line in file.lines() {
    }

    println!("Score: {score}");
}